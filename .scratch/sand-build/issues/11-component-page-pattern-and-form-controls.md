# 11: Component page pattern and form controls

**What to build:** A component page opens with a rendered demo and may carry further sections only when written. A shared page piece makes that pattern one import. The form-control components get their pages: button, input, textarea, checkbox, radio-group, switch, select, native-select, slider, label, field, input-group, input-otp.

Follow `.scratch/sand-build/page-format.md` for the page layout, the preview container, and the rules block; open the prototype there if anything is unclear.

**Blocked by:** 01 Workspace restructure, 21 Docs page skeleton and preview container

**Status:** done

- [x] a shared component-page piece: demo first, optional sections after
- [x] thirteen pages registered as written, each with at least one live demo
- [x] single-component libraries (OTP input) noted on their page as a mechanic of that component
- [x] `check` and `build` pass
