# 00: Docs page prototypes

**What to build:** One throwaway instance of each page category the docs site will repeat, so the maintainer can react to the format before any batch is written: the Overview, one foundation page (Colors or Typography), one component page (Button), one mechanics page, one preview page, and how a rule from the rules file appears on a page. Static HTML or a stub in the site, no code-quality bar. The outcome is the maintainer's feedback recorded as the page format each batch ticket follows, not the prototype itself.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] prototypes exist for Overview, one foundation page, one component page, one mechanics page, one preview page, rendered with Sand's tokens in light and dark
- [x] the maintainer has reviewed them and the feedback is written down
- [x] the agreed format for each category is recorded where the batch tickets can read it (a short section in this ticket or a notes file linked from here)
- [x] the prototype files are deleted or clearly marked throwaway

## Answer

Four prototype rounds on 2026-09-19. The agreed format is written in [page-format.md](../page-format.md); the prototype stays at [prototypes/docs-pages.html](../prototypes/docs-pages.html) as the visual reference (variant A). Kept as throwaway; nothing from it is production code.
