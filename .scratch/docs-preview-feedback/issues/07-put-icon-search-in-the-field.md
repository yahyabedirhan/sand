# 07: Put icon search in the field

**What to build:** On the Foundations Icons gallery, the search field should carry its own “Search icons” placeholder and a search glyph inside the input. Remove the separate label sitting above the field.

**Blocked by:** None (can start immediately).

**Status:** done

- [x] The gallery search no longer renders a separate visible label above the input.
- [x] The input shows “Search icons” as its placeholder.
- [x] A search icon sits inside the input and does not replace the placeholder or the typed query.
- [x] The field remains labeled for assistive technology (accessible name still describes search).
- [x] Matching, clearing, and the empty state keep working.
- [x] Automated coverage verifies the placeholder, in-field icon, and accessible name.
