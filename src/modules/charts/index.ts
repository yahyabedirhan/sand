// Charts module. The only place the chart library is imported.
// `components/ui/chart.tsx` is written against this library's API, so a swap
// changes this file and that component together.

export * from "recharts";
