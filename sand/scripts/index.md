# scripts

Commands that travel with the Sand snapshot.

- [update-sand](update-sand) - overwrite this package from a git tag and
  write that tag into `VERSION`. Run it from the workspace root as
  `./sand/scripts/update-sand <tag> <remote>`. The full sequence is in
  [installation.md](../installation.md).
