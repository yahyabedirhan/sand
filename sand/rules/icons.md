# Icon rules

## Size the icon with its control

An icon inherits the control's size, defaulting to size-4 when the parent does not set one.

## Pair an icon with text on the same line

The icon sits at the text's size, aligned to the line, not larger than the copy beside it.
