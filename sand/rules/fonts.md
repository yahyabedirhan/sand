# Font rules

## Keep serif on the top two titles only

Fraunces is legal on heading-1 and heading-2, never inside a component and never on body text.

## Use mono for code, keys, and numbers that align

Geist Mono is for inline code, keyboard keys, and tabular numbers, not for UI copy.
